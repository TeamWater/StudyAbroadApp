function doClick(e) {
    $.menu.open();
   } 
 var Cloud = require('ti.cloud');
 Cloud.debug = true;
 
 Cloud.Users.login({ 
    login: "mcritter42@gmail.com",
    password: "critter42"
}, function(e) {
    if (e.success) {
        Ti.API.info("Logged in user, id = " + users[0].id + ", session ID = " + Cloud.sessionId);
    } else {
        Ti.API.info("Login failed.");
    }
});
   
// Cloud.Events.show({
    // event_id: savedEventId
// }, function (e) {
    // if (e.success) {
        // var event = e.events[0];
        // alert('Success:\n' +
            // 'id: ' + event.id + '\n' +
            // 'name: ' + event.name + '\n' +
            // 'updated_at: ' + event.updated_at);
    // } else {
        // alert('Error:\n' +
            // ((e.error && e.message) || JSON.stringify(e)));
    // }
// });