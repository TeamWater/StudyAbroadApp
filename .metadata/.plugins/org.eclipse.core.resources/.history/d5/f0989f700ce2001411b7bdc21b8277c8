<Alloy>
	<NavigationWindow id="navWindow" platform="ios">
		<Window id="mainWindow" title="New Event" onOpen="doOpen" >
			<LeftNavButton >
				<Button id="cancelButton">Cancel</Button>
			</LeftNavButton>
			<RightNavButton>
				<Button id="saveButton">Save</Button>
			</RightNavButton>
			<TextArea id="EventContent"/>
		</Window>
	</NavigationWindow>

	<!-- ANDROID WINDOW -->
	<Window id="mainWindow" title="New Event" onOpen="doOpen"  platform="android">
	  <ScrollView>
		  <TextArea id="EventContent"/>
		</ScrollView>
	</Window>
</Alloy>